tree.tag:
		mkdir -p src
		mkdir -p src/archive
		mkdir -p src/crc
		mkdir -p src/driver
		mkdir -p src/helper
		mkdir -p src/internal
		mkdir -p src/logic
		mkdir -p src/object
		mkdir -p src/output
		mkdir -p src/skin
		mkdir -p src/util
		touch tree.tag
